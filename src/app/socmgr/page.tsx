import EmptyState from "@/components/shell/EmptyState";

export default function SocmgrDashboardPage() {
  return (
    <EmptyState
      title="Society dashboard — coming soon"
      description="The Society Manager view is being built next: your towers, your queue, and scoped exceptions for your society only. Your account and access are already set up."
    />
  );
}
